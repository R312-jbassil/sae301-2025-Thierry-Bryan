---
// filepath: src/components/configurator/ActionsConfigPanel.astro
import Button from "../ui/Button.astro";

export interface Props {
  price?: string;
}

const { price = "249€" } = Astro.props;
---

<!-- Section droite : Actions Finales (5/5) -->
<div class="bg-white flex flex-col h-full border-l border-gray-100">
  
  <!-- Contenu scrollable -->
  <div class="flex-1 overflow-y-auto p-6">
    
    <!-- Header de la section -->
    <div class="flex items-center justify-between mb-6">
      <div>
        <h2 class="font-secondary text-xl font-medium text-gray-900 mb-1">Finalisation</h2>
        <p class="font-secondary text-sm text-gray-500">Nommez et validez votre création</p>
      </div>
      <div class="bg-gray-50 px-3 py-2 rounded-lg">
        <span class="text-gray-900 font-secondary font-semibold text-lg">{price}</span>
      </div>
    </div>

    <!-- Section Nom du Modèle -->
    <div class="mb-8">
      <h3 class="font-secondary font-medium text-gray-900 mb-4">Nom du modèle</h3>
      <div class="bg-gray-50 rounded-xl p-4 border border-gray-200">
        <input 
          type="text" 
          id="model-name"
          placeholder="Ma création personnalisée..."
          class="w-full h-12 px-4 rounded-lg border border-gray-300 bg-white font-secondary text-gray-900 placeholder-gray-400 focus:border-gray-600 focus:outline-none focus:ring-2 focus:ring-gray-100 transition-all duration-200"
          maxlength="30"
        />
        <p class="text-xs text-gray-500 mt-2 font-secondary">Maximum 30 caractères</p>
      </div>
    </div>

    <!-- Résumé de la Configuration -->
    <div class="mb-8">
      <h3 class="font-secondary font-medium text-gray-900 mb-4">Résumé de votre configuration</h3>
      <div class="bg-gray-50 rounded-xl border border-gray-200 p-6 space-y-4">
        
        <!-- Matériau -->
        <div class="flex justify-between items-center py-2">
          <div class="flex items-center gap-3">
            <div class="w-6 h-6 bg-amber-100 rounded-lg flex items-center justify-center">
              <svg class="w-4 h-4 text-amber-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path d="M21 7.5l-2.25-1.313M21 7.5v2.25m0-2.25l-2.25 1.313M3 7.5l2.25-1.313M3 7.5l2.25 1.313M3 7.5v2.25m9 3l2.25-1.313L12 12.75l-2.25-1.313M12 12.75V15m0 6.75l2.25-1.313L12 21.75l-2.25-1.313M12 21.75V19.5m0 2.25l2.25-1.313M12 21.75l-2.25-1.313" stroke="currentColor" stroke-width="1.5"/>
              </svg>
            </div>
            <span class="font-secondary text-sm text-gray-600">Matériau</span>
          </div>
          <span class="font-secondary text-sm font-semibold text-gray-900" id="summary-material">Plastique</span>
        </div>
        
        <!-- Couleur Monture -->
        <div class="flex justify-between items-center py-2">
          <div class="flex items-center gap-3">
            <div class="w-6 h-6 bg-blue-100 rounded-lg flex items-center justify-center">
              <svg class="w-4 h-4 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path d="M2.036 12.322a1.012 1.012 0 010-.639C3.423 7.51 7.36 4.5 12 4.5c4.638 0 8.573 3.007 9.963 7.178.07.207.07.431 0 .639C20.577 16.49 16.64 19.5 12 19.5c-4.638 0-8.573-3.007-9.963-7.178z" stroke="currentColor" stroke-width="1.5"/>
                <path d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" stroke="currentColor" stroke-width="1.5"/>
              </svg>
            </div>
            <span class="font-secondary text-sm text-gray-600">Couleur monture</span>
          </div>
          <div class="flex items-center gap-2">
            <div class="w-4 h-4 rounded border border-gray-300" id="summary-frame-color" style="background-color: #2563eb;"></div>
            <span class="font-secondary text-sm font-semibold text-gray-900" id="summary-frame-color-name">Bleu océan</span>
          </div>
        </div>
        
        <!-- Couleur Branches -->
        <div class="flex justify-between items-center py-2">
          <div class="flex items-center gap-3">
            <div class="w-6 h-6 bg-purple-100 rounded-lg flex items-center justify-center">
              <svg class="w-4 h-4 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path d="M4.5 12.75l6 6 9-13.5" stroke="currentColor" stroke-width="1.5"/>
                <path d="M3 12h3m0 0l3-3m-3 3l3 3" stroke="currentColor" stroke-width="1"/>
                <path d="M18 12h3m0 0l-3-3m3 3l-3 3" stroke="currentColor" stroke-width="1"/>
              </svg>
            </div>
            <span class="font-secondary text-sm text-gray-600">Couleur branches</span>
          </div>
          <div class="flex items-center gap-2">
            <div class="w-4 h-4 rounded border border-gray-300" id="summary-branch-color" style="background-color: #2563eb;"></div>
            <span class="font-secondary text-sm font-semibold text-gray-900" id="summary-branch-color-name">Bleu océan</span>
          </div>
        </div>
        
        <!-- Couleur Verres -->
        <div class="flex justify-between items-center py-2">
          <div class="flex items-center gap-3">
            <div class="w-6 h-6 bg-cyan-100 rounded-lg flex items-center justify-center">
              <svg class="w-4 h-4 text-cyan-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <circle cx="12" cy="12" r="3" stroke="currentColor" stroke-width="1.5" fill="none"/>
                <path d="M12 9v6" stroke="currentColor" stroke-width="1"/>
                <path d="M9 12h6" stroke="currentColor" stroke-width="1"/>
              </svg>
            </div>
            <span class="font-secondary text-sm text-gray-600">Couleur verres</span>
          </div>
          <div class="flex items-center gap-2">
            <div class="w-4 h-4 rounded border border-gray-300" id="summary-lens-color" style="background-color: transparent;"></div>
            <span class="font-secondary text-sm font-semibold text-gray-900" id="summary-lens-color-name">Transparent</span>
          </div>
        </div>
        
        <!-- Dimensions -->
        <div class="flex justify-between items-center py-2">
          <div class="flex items-center gap-3">
            <div class="w-6 h-6 bg-emerald-100 rounded-lg flex items-center justify-center">
              <svg class="w-4 h-4 text-emerald-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path d="M3.75 6A2.25 2.25 0 016 3.75h2.25A2.25 2.25 0 0110.5 6v2.25a2.25 2.25 0 01-2.25 2.25H6a2.25 2.25 0 01-2.25-2.25V6zM3.75 15.75A2.25 2.25 0 016 13.5h2.25a2.25 2.25 0 012.25 2.25V18a2.25 2.25 0 01-2.25 2.25H6A2.25 2.25 0 013.75 18v-2.25zM13.5 6a2.25 2.25 0 012.25-2.25H18A2.25 2.25 0 0120.25 6v2.25A2.25 2.25 0 0118 10.5h-2.25a2.25 2.25 0 01-2.25-2.25V6zM13.5 15.75a2.25 2.25 0 012.25-2.25H18a2.25 2.25 0 012.25 2.25V18A2.25 2.25 0 0118 20.25h-2.25A2.25 2.25 0 0113.5 18v-2.25z" stroke="currentColor" stroke-width="1.5"/>
              </svg>
            </div>
            <span class="font-secondary text-sm text-gray-600">Dimensions</span>
          </div>
          <span class="font-secondary text-sm font-semibold text-gray-900" id="summary-dimensions">52mm × 18mm</span>
        </div>
      </div>
    </div>





  </div>

  <!-- Boutons de navigation (toujours visibles) -->
  <div class="bg-white border-t border-gray-100 p-6">
    <div class="flex justify-between items-center">
      <button id="prev-step" class="flex items-center gap-2 text-gray-600 font-secondary font-medium transition-all duration-200 hover:text-gray-900 focus:outline-none">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" />
        </svg>
        Précédent
      </button>
      
      <button id="add-to-cart" class="flex h-10 px-6 items-center rounded-lg bg-gray-900 text-white font-secondary font-medium transition-all duration-200 hover:bg-gray-800 hover:shadow-md">
        <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2">
          <path stroke-linecap="round" stroke-linejoin="round" d="M2.25 3h1.386c.51 0 .955.343 1.087.835l.383 1.437M7.5 14.25a3 3 0 00-3 3h15.75m-12.75-3h11.218c1.121-2.3 2.1-4.684 2.924-7.138a60.114 60.114 0 00-16.536-1.84M7.5 14.25L5.106 5.272M6 20.25a.75.75 0 11-1.5 0 .75.75 0 011.5 0zm12.75 0a.75.75 0 11-1.5 0 .75.75 0 011.5 0z"/>
        </svg>
        Ajouter au panier
      </button>
    </div>
  </div>

</div>

<script>
  // Gestion des actions finales
  document.addEventListener('DOMContentLoaded', () => {
    const addToCartBtn = document.querySelector('#add-to-cart');
    const modelNameInput = document.querySelector('#model-name') as HTMLInputElement;
    const finalPriceElement = document.querySelector('#final-price');

    // Éléments du résumé
    const summaryMaterial = document.querySelector('#summary-material');
    const summaryFrameColor = document.querySelector('#summary-frame-color') as HTMLElement;
    const summaryFrameColorName = document.querySelector('#summary-frame-color-name');
    const summaryBranchColor = document.querySelector('#summary-branch-color') as HTMLElement;
    const summaryBranchColorName = document.querySelector('#summary-branch-color-name');
    const summaryLensColor = document.querySelector('#summary-lens-color') as HTMLElement;
    const summaryLensColorName = document.querySelector('#summary-lens-color-name');
    const summaryDimensions = document.querySelector('#summary-dimensions');

    // Données de configuration (mises à jour en temps réel)
    let configurationData = {
      nom_modele: '',
      id_materiau: 1,
      materiau_nom: 'Plastique',
      couleur_monture: '#2563eb',
      couleur_monture_nom: 'Bleu océan',
      couleur_branches: '#2563eb',
      couleur_branches_nom: 'Bleu océan',
      couleur_verres: 'transparent',
      couleur_verres_nom: 'Transparent',
      largeur_pont: 18,
      taille_verres: 52,
      price: 249
    };

    // Exposer configurationData globalement pour l'Assistant IA
    (window as any).configurationData = configurationData;

    // Fonction pour mettre à jour le résumé de configuration
    function updateConfigurationSummary() {
      if (summaryMaterial) {
        summaryMaterial.textContent = configurationData.materiau_nom;
      }
      
      if (summaryFrameColor && summaryFrameColorName) {
        summaryFrameColor.style.backgroundColor = configurationData.couleur_monture;
        summaryFrameColorName.textContent = configurationData.couleur_monture_nom;
      }
      
      if (summaryBranchColor && summaryBranchColorName) {
        summaryBranchColor.style.backgroundColor = configurationData.couleur_branches;
        summaryBranchColorName.textContent = configurationData.couleur_branches_nom;
      }
      
      if (summaryLensColor && summaryLensColorName) {
        summaryLensColor.style.backgroundColor = configurationData.couleur_verres === 'transparent' ? 'rgba(255,255,255,0.8)' : configurationData.couleur_verres;
        summaryLensColor.style.border = configurationData.couleur_verres === 'transparent' ? '1px dashed #d1d5db' : '1px solid #d1d5db';
        summaryLensColorName.textContent = configurationData.couleur_verres_nom;
      }
      
      if (summaryDimensions) {
        summaryDimensions.textContent = `${configurationData.taille_verres}mm × ${configurationData.largeur_pont}mm`;
      }
    }

    // Écouter les événements de changement de couleur de la monture
    window.addEventListener('frameColorChange', (e) => {
      const event = e as CustomEvent;
      const color = event.detail.color;
      
      configurationData.couleur_monture = color;
      
      // Mapping des couleurs vers les noms
      const colorNames = {
        '#2563eb': 'Bleu océan',
        '#7c3aed': 'Violet moderne',
        '#059669': 'Vert émeraude',
        '#dc2626': 'Rouge cerise',
        '#ea580c': 'Orange mandarine',
        '#0891b2': 'Cyan tropical',
        '#4338ca': 'Indigo profond',
        '#374151': 'Gris anthracite'
      };
      
      configurationData.couleur_monture_nom = colorNames[color as keyof typeof colorNames] || 'Couleur personnalisée';
      updateConfigurationSummary();
    });

    // Écouter les événements de changement de couleur des branches
    window.addEventListener('branchColorChange', (e) => {
      const event = e as CustomEvent;
      const color = event.detail.color;
      
      configurationData.couleur_branches = color;
      
      const colorNames = {
        '#2563eb': 'Bleu océan',
        '#7c3aed': 'Violet moderne',
        '#059669': 'Vert émeraude',
        '#dc2626': 'Rouge cerise',
        '#ea580c': 'Orange mandarine',
        '#0891b2': 'Cyan tropical',
        '#4338ca': 'Indigo profond',
        '#374151': 'Gris anthracite'
      };
      
      configurationData.couleur_branches_nom = colorNames[color as keyof typeof colorNames] || 'Couleur personnalisée';
      updateConfigurationSummary();
    });

    // Écouter les événements de changement de matériau
    window.addEventListener('materialChange', (e) => {
      const event = e as CustomEvent;
      const { materialId, materialName } = event.detail;
      
      configurationData.id_materiau = materialId;
      configurationData.materiau_nom = materialName;
      updateConfigurationSummary();
    });

    // Écouter les événements de changement de couleur des verres
    window.addEventListener('lensColorChange', (e) => {
      const event = e as CustomEvent;
      const { color, fill, opacity } = event.detail;
      
      // Mapping des couleurs de verres
      const lensColorNames = {
        'transparent': 'transparent',
        'gray': '#6b7280',
        'dark-gray': '#374151',
        'brown': '#92400e',
        'green': '#166534',
        'blue': '#1e40af',
        'yellow': '#eab308',
        'pink': '#ec4899'
      };

      const lensColorDisplayNames = {
        'transparent': 'Transparent',
        'gray': 'Gris clair',
        'dark-gray': 'Gris foncé',
        'brown': 'Marron',
        'green': 'Vert',
        'blue': 'Bleu',
        'yellow': 'Jaune',
        'pink': 'Rose'
      };
      
      // Stocker la couleur des verres
      configurationData.couleur_verres = lensColorNames[color as keyof typeof lensColorNames] || 'transparent';
      configurationData.couleur_verres_nom = lensColorDisplayNames[color as keyof typeof lensColorDisplayNames] || 'Transparent';
      
      updateConfigurationSummary();
      console.log('Couleur des verres mise à jour:', configurationData.couleur_verres, configurationData.couleur_verres_nom);
    });

    // Écouter les événements de changement de dimensions
    window.addEventListener('dimensionChange', (e) => {
      const event = e as CustomEvent;
      const { type, value } = event.detail;
      
      if (type === 'lens-width') {
        configurationData.taille_verres = value;
      } else if (type === 'bridge-width') {
        configurationData.largeur_pont = value;
      }
      
      updateConfigurationSummary();
    });

    // Initialiser le résumé au chargement
    updateConfigurationSummary();

    // Fonction pour calculer le prix final (prix fixe pour le moment)
    function updateFinalPrice() {
      if (finalPriceElement) {
        finalPriceElement.textContent = `${configurationData.price}€`;
      }
    }

    // Bouton Ajouter au panier
    if (addToCartBtn) {
      addToCartBtn.addEventListener('click', async () => {
        try {
          const modelName = modelNameInput?.value || 'Ma création';

          // Générer le SVG personnalisé avec les couleurs directement intégrées
          const couleurMonture = configurationData.couleur_monture;
          const couleurBranches = configurationData.couleur_branches;
          const couleurVerres = configurationData.couleur_verres;
          
          console.log('🎨 Génération SVG avec couleurs - Monture:', couleurMonture, 'Branches:', couleurBranches, 'Verres:', couleurVerres);
          
          // Créer un SVG personnalisé avec les vraies couleurs
          const svgCode = `<svg width="300" height="150" viewBox="0 120 600 200" xmlns="http://www.w3.org/2000/svg">
            <g id="branches">
              <path fill="${couleurBranches}" stroke="${couleurBranches}" stroke-miterlimit="10" stroke-width="0.5" d="M108.83,155.14s49.35-5.57,54.93-8.76,11.14-7.96,19.5-9.55,64.48-11.14,64.48-11.14c0,0,7.16-1.99,17.51,5.57s33.04,31.84,39.4,31.05c6.37-.8,9.55-9.55,7.56-13.53s-40.67-32.75-47.36-34.63c-9.53-2.68-21.89-2.39-37.02,0-15.12,2.39-116.22,23.34-116.22,23.34,0,0-13.13,6.12-2.79,17.66Z"/>
              <path fill="#706f6f" d="M308.24,150.76s-.31,6.15-3.98,5.57c-4.97-.78-35.82-33.83-46.17-32.24,0,0,8.53,2.71,18.46,11.64,6.51,5.86,34.42,37.85,31.69,15.03Z"/>
              <path fill="${couleurBranches}" stroke="${couleurBranches}" stroke-miterlimit="10" stroke-width="0.5" d="M374.79,179.65l90.54-29.78s22.13-5.63,25.35-4.02,17.71,30.58,17.71,33.8-2.42,14.01-5.63,14.89c-2.33.64-5.33.11-6.84-4.21-1.41-4.02-6.84-31.73-12.47-31.47s-50.7,15.95-50.7,15.95c0,0-13.28,6.44-18.91,11.27s-32.19,11.27-32.19,11.27c0,0-9.66-7.65-6.84-17.71Z"/>
              <path fill="#706f6f" d="M381.34,180.1s102.27-35.19,107.85-31.99c0,0-18.2,2.49-41.76,11.88s-66.09,22.03-66.09,22.03v-1.92Z"/>
              <path fill="#706f6f" d="M485.36,158.06s3.26,0,4.98,3.45,9.77,24.71,9.77,24.71c0,0,.77,4.98,5.17-2.68,0,0-3.07,9.56-5.36,6.79s-9.77-26.33-9.77-26.33c0,0-2.87-6.74-4.79-5.94Z"/>
            </g>
            <g id="monture">
              <path fill="${couleurMonture}" stroke="${couleurMonture}" stroke-miterlimit="10" stroke-width="0.5" d="M385.31,180.81c-.24-3.41-9.57-5.22-12.71-6.4-3.14-1.18-15.7,1.96-15.7,1.96-12.17-3.14-54.55-9.03-78.88-9.03s-32.57,11.38-35.32,10.99c-2.75-.39-17.27-.78-23.55-1.96-6.28-1.18-5.1-14.13-39.25-26.48-34.14-12.35-68.29-12.42-68.29-12.42-13.35,4.66-6.76,18.49-6.76,18.49l2.05,6.35c4.32,2.03,3.14,10.52,4.71,23.08s7.46,28.65,14.13,38.46c6.67,9.81,31.79,20.8,44.35,21.19s23.55-1.96,31.79-15.31c8.24-13.34,16.88-36.89,16.88-36.89l10.2,1.57s2.91.31,5.35,3.09,4.43,8.03,7.6,17.31c7.56,22.1,25.12,39.25,25.12,39.25,0,0,5.6,7.43,33.44,12.66,23.08,4.34,42.7-2.46,42.7-2.46,17.72-7.17,21.63-44.96,21.63-44.96,0,0,1.83-13.3,7.81-16.65,7.37-4.14,8.27-4.44,12.71-5.32,3.61-.71.24-13.13,0-16.54Z"/>
            </g>
            <g id="verres">
              <path fill="${couleurVerres}" stroke="#d1d5db" stroke-width="1" d="M116.32,162.87c-.88,6.05.4,37.41,11.44,55.31s37.04,21.35,41.37,21.31,18.51,1.02,24.47-9.27c4.97-8.59,13.17-43.48,9.12-53.05-6.26-14.82-40.4-24.02-40.4-24.02,0,0-26.85-5.46-33.82-4.46,0,0-9.97-.96-12.17,14.17Z"/>
              <path fill="${couleurVerres}" stroke="#d1d5db" stroke-width="1" d="M247.32,192.54s-1.09-9.55,19.57-16.69c11.76-4.06,60.46,3.23,60.46,3.23,0,0,19.09,4.06,21.71,8.28,2.62,4.22,9.46,17.24,8.29,37.97-1.14,20.21-7.31,26.8-12.61,30.31,0,0-7.1,4.93-21.34,5.93-26.65,1.88-37.01-1.14-46.19-7.73-29.88-21.46-29.88-61.3-29.88-61.3Z"/>
            </g>
          </svg>`;

          // Préparer les données pour PocketBase
          const lunetteConfig = {
            nom_modele: modelName,
            couleur_verres: configurationData.couleur_verres,
            couleur_monture: configurationData.couleur_monture,
            couleur_branches: configurationData.couleur_branches,
            largeur_pont: configurationData.largeur_pont,
            taille_verres: configurationData.taille_verres,
            id_materiau: configurationData.id_materiau.toString(),
            code_svg: svgCode
          };

          // Afficher un indicateur de chargement
          const button = addToCartBtn as HTMLButtonElement;
          button.textContent = 'Enregistrement...';
          button.disabled = true;

          console.log('📋 Configuration complète à enregistrer:', lunetteConfig);
          console.log('📋 État complet configurationData:', configurationData);

          // Envoyer les données via un événement personnalisé
          const saveEvent = new CustomEvent('saveLunette', {
            detail: lunetteConfig
          });
          window.dispatchEvent(saveEvent);
          
          // Afficher une confirmation de succès
          showNotification('✅ Configuration préparée pour l\'enregistrement !', 'success');

        } catch (error: any) {
          console.error('Erreur lors de la préparation:', error);
          const errorMessage = error?.message || 'Erreur inconnue';
          showNotification('❌ Erreur lors de la préparation: ' + errorMessage, 'error');
        } finally {
          // Restaurer le bouton
          const button = addToCartBtn as HTMLButtonElement;
          if (button) {
            button.textContent = 'Ajouter au panier';
            button.disabled = false;
          }
        }
      });
    }





    // Fonction pour afficher les notifications
    function showNotification(message: string, type: 'success' | 'info' | 'error') {
      const notification = document.createElement('div');
      notification.className = `fixed top-4 right-4 p-4 rounded-lg text-white font-secondary text-sm z-50 transition-opacity duration-300 ${
        type === 'success' ? 'bg-green-600' : 
        type === 'info' ? 'bg-blue-600' : 'bg-red-600'
      }`;
      notification.textContent = message;
      
      document.body.appendChild(notification);
      
      setTimeout(() => {
        notification.style.opacity = '0';
        setTimeout(() => {
          document.body.removeChild(notification);
        }, 300);
      }, 3000);
    }

    // Initialiser le prix
    updateFinalPrice();

    // Écouter l'événement de chargement d'une création existante
    window.addEventListener('loadExistingCreation', (e) => {
      const customEvent = e as CustomEvent;
      const lunette = customEvent.detail;
      console.log('📝 ActionsConfigPanel: Chargement de la création existante:', lunette);

      // Charger le nom du modèle
      if (lunette.nom_modele) {
        const modelNameInput = document.querySelector('#model-name') as HTMLInputElement;
        if (modelNameInput) {
          modelNameInput.value = lunette.nom_modele;
          
          // Dispatcher l'événement de changement de nom
          window.dispatchEvent(new CustomEvent('modelNameChange', {
            detail: { name: lunette.nom_modele }
          }));
        }
      }

      // Mettre à jour le récapitulatif avec toutes les données
      updateConfigurationSummary();
      showNotification('📝 Configuration chargée pour modification', 'info');
    });
  });
</script>